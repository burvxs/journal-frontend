import React from 'react';

const WeeklyReview = (props) => {
    return (
        <div>
            <textarea name="review" rows="8" cols="60"/>
        </div>
    );
}

export default WeeklyReview;
